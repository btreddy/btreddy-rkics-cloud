{
  "name": "rkics-supervisor-tracker-cloud",
  "version": "1.0.0",
  "description": "RkICS site supervisor tracker - cloud version (Vercel + Supabase)",
  "main": "api/telegram.js",
  "scripts": {
    "setup": "node setup.js",
    "transcribe": "node transcribe.js",
    "digest:test": "node -e \"require('dotenv').config(); require('./lib/core').buildDailyDigest().then(t=>{console.log(t); process.exit(0)})\""
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "pg": "^8.13.0",
    "xlsx": "^0.18.5"
  }
}
